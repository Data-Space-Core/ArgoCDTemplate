apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: connector

resources:
  - namespace.yaml
  - connector-secret.yaml
  - connector-configmap.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - connector-deployment.yaml
  - connector-service.yaml